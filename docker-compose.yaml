services:
  app:
    build:
      context: .
      dockerfile: ./src/backend/dockerfile
    ports:
      - "8080:8080"
    command: uvicorn src.backend.app.Main:app --host 0.0.0.0 --port 8080 --reload
